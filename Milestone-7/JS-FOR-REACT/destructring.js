const number = [1, 2];

const [x,y] = number;

console.log(x,y);

const product = 
    {
      name: "laptop",
      price: 1222,
      brand: "Hp",
      color: ['red', 'green', 'blue'],
    }

    const [a,b,c] = product.color;
    console.log(a,b,c);


    const {name,age} = {}


const computer ={
    name : "hp",
    prise : 35000,
    j: {
        x : 12,
    }

}

const keys = Object.keys(computer);
console.log(keys);

const obj = Object.values(computer.j);

console.log(obj)